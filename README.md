# OTP Mail Notifier

Safe demo Android app that detects OTP from pasted email text and shows a notification.

Next version can add Gmail OAuth + Gmail readonly API after creating Google Cloud OAuth credentials.
